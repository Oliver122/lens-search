# Progress — <slug>

States: `todo` | `in-progress` | `done` | `blocked` | `gap`

This file in `requirements/<slug>/` is a legend only. Do not mark live status here (that would change `CYCLE`).

During a cycle the coder writes **`PROGRESS.md` at the repo root** on `auto-feature/<slug>` and commits after each test so the MR updates.

| # | specified test | state |
|---|---|---|
| 1 |  | todo |
