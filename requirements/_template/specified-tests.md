# Specified tests — <slug>

Each item is an observable check. A coder treats an unmet item as a gap (`GAPS.md`), not as a license to widen the spec.

Tag each line with one closed slice: `ui` | `backend` | `process`.

1. [process] 
