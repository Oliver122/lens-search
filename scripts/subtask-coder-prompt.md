Implement only the single specified-test subtask below, directly on this checkout.

You are working on the accumulated code of the subtasks before yours. Build on it; do not restructure or rewrite what already works.

Read `requirements/<slug>/overview.md`. Implement until this subtask’s specified test can pass, and add an automated test that proves it. The whole existing test suite must still pass afterwards; do not break existing tests. Do not add requirements. Do not edit files under `requirements/<slug>/` (that would change CYCLE). Do not edit `PROGRESS.md` (the orchestrator owns that board) or `GAPS.md` (the orchestrator records gaps after verification).

Do not implement other specified tests. Do not load sibling subtask prompts or transcripts.

Do not `git push`. Do not merge to main. After you finish, the orchestrator verifies your work (build + full test suite), then commits and pushes it.

If the spec is too thin to implement this subtask without guessing, do not invent Gherkin or extra requirements. Stop without guessing.
